#pragma once 

namespace consts
{
    const int maxTables = 3;
    const int maxPlayersPerTable = 3;
    const int playerStartBalance = 1000;
    const int shoeDecks = 3;
    const int waitingSeconds = 1;
    
} // namespace consts